// BackgroundAudio removed — `BackgroundMusic` provides unified music controls (YouTube + local files).
// If you need to re-enable a minimal floating audio control, implement it in `BackgroundMusic` instead.

export default null;
